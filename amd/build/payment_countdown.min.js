/**
 * Xử lý chuyển hướng đếm ngược thanh toán SePay.
 *
 * @module     enrol_sepay/payment_countdown
 * @copyright  2026 Quiz Van Lang <quizvanlang@gmail.com>
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("enrol_sepay/payment_countdown",[],(function(){return{init:function(courseId,enrolUrl){const storageKey="sepay_processed_countdown_"+courseId;let remainingSeconds,startTime=localStorage.getItem(storageKey);if(startTime){const elapsed=Math.floor((Date.now()-parseInt(startTime,10))/1e3);remainingSeconds=Math.max(0,5-elapsed)}else startTime=Date.now(),localStorage.setItem(storageKey,startTime),remainingSeconds=5;if(remainingSeconds<=0)return localStorage.removeItem(storageKey),void(window.location.href=enrolUrl);const countdownElement=document.getElementById("countdown");countdownElement&&(countdownElement.textContent=remainingSeconds);let seconds=remainingSeconds;const interval=setInterval((function(){seconds--,countdownElement&&(countdownElement.textContent=seconds),seconds<=0&&(clearInterval(interval),localStorage.removeItem(storageKey),window.location.href=enrolUrl)}),1e3)}}}));

//# sourceMappingURL=payment_countdown.min.js.map